services:

## Router
  router:
    build: 
      context: mongodb-build
    container_name: router
    command: mongos --port 27017 --configdb rs-config-server/192.168.30.91:25100,192.168.30.92:25100,192.168.30.93:25100 --bind_ip_all --keyFile /data/mongodb-keyfile
    ports:
      - 25200:27017
    restart: always
    volumes:
      - ./scripts:/scripts
      - mongodb_cluster_router_db:/data/db
      - mongodb_cluster_router_config:/data/configdb

## Config Servers
  configsvr:
    build: 
      context: mongodb-build
    container_name: configsvr
    command: mongod --port 27017 --configsvr --replSet rs-config-server --bind_ip_all --keyFile /data/mongodb-keyfile
    volumes:
      - ./scripts:/scripts 
      - mongodb_cluster_configsvr_db:/data/db
      - mongodb_cluster_configsvr_config:/data/configdb
    ports:
      - 25100:27017
    restart: always

## Shards
  shard-01:
    build: 
      context: mongodb-build
    container_name: shard-01
    command: mongod --port 27017 --shardsvr --replSet rs-shard-01 --bind_ip_all --keyFile /data/mongodb-keyfile
    volumes:
      - ./scripts:/scripts
      - mongodb_cluster_shard_01_db:/data/db
      - mongodb_cluster_shard_01_config:/data/configdb
    ports:
      - 25000:27017
    restart: always
  shard-02:
    build: 
      context: mongodb-build
    container_name: shard-02
    command: mongod --port 27017 --shardsvr --replSet rs-shard-02 --bind_ip_all --keyFile /data/mongodb-keyfile
    volumes:
      - ./scripts:/scripts
      - mongodb_cluster_shard_02_db:/data/db
      - mongodb_cluster_shard_02_config:/data/configdb
    ports:
      - 25001:27017
    restart: always
  shard-03:
    build: 
      context: mongodb-build
    container_name: shard-03
    command: mongod --port 27017 --shardsvr --replSet rs-shard-03 --bind_ip_all --keyFile /data/mongodb-keyfile
    volumes:
      - ./scripts:/scripts
      - mongodb_cluster_shard_03_db:/data/db
      - mongodb_cluster_shard_03_config:/data/configdb
    ports:
      - 25002:27017
    restart: always

volumes:
  mongodb_cluster_router_db:
  mongodb_cluster_router_config:
  
  mongodb_cluster_configsvr_db:
  mongodb_cluster_configsvr_config:
  
  mongodb_cluster_shard_01_db:
  mongodb_cluster_shard_01_config:
  
  mongodb_cluster_shard_02_db:
  mongodb_cluster_shard_02_config:
  
  mongodb_cluster_shard_03_db:
  mongodb_cluster_shard_03_config:
